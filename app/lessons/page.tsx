"use client";
import Link from "next/link";import {useMemo,useState,useEffect,Suspense} from "react";import {useSearchParams} from "next/navigation";import Sidebar from "@/components/Sidebar";import TopBar from "@/components/TopBar";import LessonCard from "@/components/LessonCard";import {categories,lessons} from "@/lib/lessons";import {loadProgress} from "@/lib/progress";
function Content(){const sp=useSearchParams();const q=(sp.get('q')||'').toLowerCase();const [open,setOpen]=useState(false);const [cat,setCat]=useState('الكل');const [s,setS]=useState(loadProgress());useEffect(()=>{const f=()=>setS(loadProgress());window.addEventListener('storage',f);window.addEventListener('alifya:progress-updated',f);return()=>{window.removeEventListener('storage',f);window.removeEventListener('alifya:progress-updated',f)}},[]);const filtered=useMemo(()=>lessons.filter(l=>(cat==='الكل'||l.category===cat)&&(!q||`${l.title} ${l.arabicTitle} ${l.description} ${l.vocabulary.map(v=>`${v.arabic} ${v.meaning}`).join(' ')}`.toLowerCase().includes(q))),[cat,q]);return <div><TopBar onMenuClick={()=>setOpen(true)}/><div className="layout"><Sidebar isOpen={open} onClose={()=>setOpen(false)}/><main className="main"><div className="section-head" style={{marginTop:4}}><div><span className="eyebrow">المنهج</span><h2>الدروس</h2><span className="muted">{q?`نتائج البحث عن «${q}»`:"اختر جلسة وابدأ فوراً."}</span></div><span className="pill">{filtered.length} نتيجة</span></div><div className="segmented" style={{display:'inline-flex',marginBottom:18}}>{['الكل',...categories].map(c=><button key={c} onClick={()=>setCat(c)} className={cat===c?'active':''}>{c}</button>)}</div>{filtered.length?<div className="lesson-grid">{filtered.map(l=><LessonCard key={l.id} lesson={l} progress={s.lessons[l.id]}/>)}</div>:<div className="surface-card" style={{textAlign:'center',padding:50}}><div style={{fontSize:40}}>🔎</div><h3>لا توجد نتائج</h3><p className="muted">جرّب كلمة أخرى أو اختر تصنيفاً مختلفاً.</p><Link className="btn secondary" href="/lessons">إظهار الكل</Link></div>}</main></div></div>}
export default function LessonsPage(){return <Suspense fallback={<div className="main">جارِ التحميل…</div>}><Content/></Suspense>}
