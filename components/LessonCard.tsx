"use client";import type {CurriculumLesson} from "@/lib/curriculum";import type {LessonState} from "@/lib/progress";import {useLanguage} from "@/lib/i18n";
export default function LessonCard({lesson,progress}:{lesson:CurriculumLesson;progress?:LessonState}){const{isArabic}=useLanguage();const pct=progress?.bestScore??0;return <a href={`/lessons/${lesson.id}`} className="lesson-card"><div className="lesson-icon">{lesson.icon}</div><div className="lesson-copy"><div className="row"><span className="cefr-badge mini">{lesson.cefr}</span><span className="skill-tag">{lesson.skill}</span><span className="muted">{lesson.durationEn} min</span></div><h3>{isArabic?lesson.arabicTitle:lesson.title}</h3><p>{isArabic?lesson.title:lesson.arabicTitle}</p><div className="lesson-unit">{lesson.unit}</div><div className="card-progress"><span><i style={{width:`${pct}%`}}/></span><b>{progress?.completed?`${pct}%`:(isArabic?'ابدأ':'Start')}</b></div></div></a>}
