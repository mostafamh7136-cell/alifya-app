"use client";
import Link from "next/link"; import type {Lesson} from "@/lib/lessons"; import type {LessonState} from "@/lib/progress";
export default function LessonCard({lesson,progress}:{lesson:Lesson;progress?:LessonState}){const pct=progress?.bestScore??0;return <Link href={`/lessons/${lesson.id}`} className="lesson-card"><div className="lesson-icon">{lesson.icon}</div><div className="lesson-copy"><div className="row"><span className={`level level-${lesson.levelEn}`}>{lesson.level}</span><span className="muted">{lesson.duration}</span></div><h3>{lesson.arabicTitle}</h3><p>{lesson.title}</p><div className="card-progress"><span><i style={{width:`${pct}%`}}/></span><b>{progress?.completed?`${pct}%`:'ابدأ'}</b></div></div></Link>}
