"use client";
import Link from "next/link"; import {usePathname,useRouter} from "next/navigation"; import {useState} from "react"; import {useTheme} from "./ThemeProvider";
export default function TopBar({onMenuClick}:{onMenuClick:()=>void}){const path=usePathname();const router=useRouter();const {theme,toggleTheme}=useTheme();const [q,setQ]=useState("");const submit=(e:React.FormEvent)=>{e.preventDefault();router.push(q.trim()?`/lessons?q=${encodeURIComponent(q.trim())}`:"/lessons")};return <header className="topbar"><button className="icon-btn mobile-only" onClick={onMenuClick} aria-label="فتح القائمة">☰</button><Link href="/" className="brand"><span>ا</span><div><b>أليفا</b><small>Alifya · Language Lab</small></div></Link><form onSubmit={submit} className="search"><span>⌕</span><input aria-label="بحث" value={q} onChange={e=>setQ(e.target.value)} placeholder="ابحث عن كلمة أو درس…"/></form><div className="top-actions"><button className="icon-btn" onClick={toggleTheme} aria-label="تبديل المظهر">{theme==="dark"?"☀️":"🌙"}</button><Link className="profile" href="/progress" aria-label="ملفك وتقدمك">م</Link></div></header>}
