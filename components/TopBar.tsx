"use client";
import Link from "next/link";import {usePathname,useRouter} from "next/navigation";import {useEffect,useState} from "react";import {useTheme} from "./ThemeProvider";import {useLanguage} from "@/lib/i18n";
export default function TopBar({onMenuClick}:{onMenuClick:()=>void}){const path=usePathname();const router=useRouter();const {theme,toggleTheme}=useTheme();const {lang,toggleLang,isArabic}=useLanguage();const [q,setQ]=useState("");useEffect(()=>{const t=setTimeout(()=>{const v=q.trim();router.replace(v?`/lessons?q=${encodeURIComponent(v)}`:"/lessons")},220);return()=>clearTimeout(t)},[q,router]);return <header className="topbar"><button className="icon-btn mobile-only" onClick={onMenuClick} aria-label={isArabic?"فتح القائمة":"Open menu"}>☰</button><Link href="/" className="brand"><span>ا</span><div><b>Alifya</b><small>Arabic Language Learning</small></div></Link><form onSubmit={e=>e.preventDefault()} className="search"><span>⌕</span><input aria-label={isArabic?"بحث":"Search"} value={q} onChange={e=>setQ(e.target.value)} placeholder={isArabic?"ابحث عن كلمة أو درس…":"Search lessons or words…"}/></form><div className="top-actions"><button className="icon-btn" onClick={toggleLang} aria-label={isArabic?"Switch to English":"Switch to Arabic"}>{lang==="en"?"عربي":"EN"}</button><button className="icon-btn" onClick={toggleTheme} aria-label={isArabic?"تبديل المظهر":"Toggle theme"}>{theme==="dark"?"☀️":"🌙"}</button><Link className="profile" href="/progress" aria-label={isArabic?"ملفك وتقدمك":"Your progress"}>M</Link></div></header>}
